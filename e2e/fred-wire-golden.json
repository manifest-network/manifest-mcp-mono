{
  "_comment": [
    "Observed Fred tenant-response key sets, captured from the live devnet (ENG-638).",
    "",
    "This is the ONLY artifact in the repo derived from what Fred actually sends rather",
    "than from reading its Go by hand. Unit fixtures and mono's TypeScript interfaces are",
    "two hand-mirrors that can agree with each other while both being wrong about the",
    "provider -- which is exactly how Fred's ENG-508 removal of `last_error` stayed",
    "invisible to a green CI. Only an observation can notice that.",
    "",
    "WHAT IS ACTUALLY OBSERVABLE. e2e drives the MCP tools, not the provider HTTP API",
    "(the test client speaks stdio MCP and holds no wallet, so it cannot mint an ADR-036",
    "token for a raw fetch). Two of the three surfaces still expose Fred's own keys:",
    "app_status.fredStatus and app_releases.releases[] are zod looseObjects fed from the",
    "raw provider response, so unmodelled Fred keys pass through and ARE visible here.",
    "app_diagnostics is an explicit projection, so its key set is mono's contract rather",
    "than Fred's -- recorded as such below, and it cannot detect a field Fred adds.",
    "",
    "`always`: keys expected on every successful call for the lease state the e2e flow",
    "exercises (a healthy ACTIVE lease). A missing one FAILS -- that is the ENG-508 class",
    "of break, where mono reads a field the provider stopped sending.",
    "",
    "`sometimes`: legitimately conditional keys. Failure attribution appears only once a",
    "failure has been recorded; retention keys only on a retained lease. Absence is not a",
    "signal. But a key in NEITHER list is, because it means Fred grew a field mono does",
    "not model.",
    "",
    "`mono_strips`: keys Fred sends that mono deliberately removes before the model sees",
    "them, so their absence here is correct and must not be read as provider drift.",
    "",
    "`mono_adds`: the mirror image -- keys mono DERIVES that Fred never sends, so their",
    "presence is also not provider drift. Without this they would read as an unmodelled",
    "Fred field, which is the opposite of the truth.",
    "",
    "When this fails, do not edit this file to make it pass. Work out which side moved:",
    "if Fred dropped a key mono reads, that is a client fix; if Fred added one, decide",
    "whether to model it, then record it here."
  ],
  "fred_ref": "8f0cbd9431b482732d60d81fb59f94a37cd06486",
  "fred_ref_note": "Original capture revision for the baseline and conditional-field allowlists. Later live observations are recorded separately below.",

  "status": {
    "endpoint": "GET /v1/leases/{uuid}/status",
    "observed_via": "app_status.fredStatus (looseObject — carries Fred's own keys)",
    "always": [
      "lease_uuid",
      "tenant",
      "provider_uuid",
      "state",
      "requires_payload",
      "payload_received",
      "provisioning_started",
      "provision_status"
    ],
    "sometimes": [
      "meta_hash_hex",
      "fail_count",
      "reason",
      "message",
      "retained_until",
      "items",
      "restore_hint"
    ],
    "mono_strips": ["partition"],
    "mono_adds": []
  },

  "release": {
    "endpoint": "GET /v1/leases/{uuid}/releases -> releases[]",
    "observed_via": "app_releases.releases[] (looseObject — carries Fred's own keys)",
    "always": ["version", "image", "status", "created_at"],
    "sometimes": ["reason", "message"],
    "mono_strips": ["manifest"],
    "mono_adds": ["manifest_bytes"]
  },

  "diagnostics_projection": {
    "endpoint": "GET /v1/leases/{uuid}/provision",
    "observed_via": "app_diagnostics structuredContent (EXPLICIT projection — mono's contract, not Fred's; cannot detect a field Fred adds)",
    "always": ["lease_uuid", "lease_state", "provision_status", "fail_count"],
    "sometimes": [
      "reason",
      "message",
      "last_error",
      "retained_until",
      "items",
      "restore_hint"
    ],
    "mono_strips": ["tenant", "provider_uuid", "partition"],
    "mono_adds": ["lease_state", "next_step"]
  },

  "live_observations": {
    "fred_ref": "4f00091cd7ace41c92bb2d1ebcd2c1a68fb7d234",
    "mono_ref": "5d7020c77a0d288bf15d1ea969f055e8a0addb20",
    "run_url": "https://github.com/manifest-network/manifest-mcp-mono/actions/runs/35367039071",
    "scope": "Healthy ACTIVE status and release keys observed in live lifecycle tests. Conditional-field allowlists retain their original baseline provenance. This run passed maintenance replay/conflict but failed the diagnostics projection check and stateful deployment; it is not full restore acceptance.",
    "status": [
      "lease_uuid",
      "meta_hash_hex",
      "payload_received",
      "provider_uuid",
      "provision_status",
      "provisioning_started",
      "requires_payload",
      "state",
      "tenant"
    ],
    "release": ["created_at", "image", "manifest_bytes", "status", "version"],
    "diagnostics_projection_correction": "The live assertion observed lease_state as an extra key. It is always derived from chain state by mono, not added by Fred."
  }
}
